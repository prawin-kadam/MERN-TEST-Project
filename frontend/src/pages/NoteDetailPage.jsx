import React from 'react'
import { useEffect } from 'react'

const NoteDetailPage = () => {

  const [notes,setNotes] = useEffect(null);
  const [loading , setLoading] =  useEffect(true);
  const [saving,]
  return (
    <div>NoteDetailPage</div>
  )
}

export default NoteDetailPage